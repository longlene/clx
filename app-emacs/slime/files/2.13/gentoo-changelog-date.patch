--- slime-2.13.orig/slime.el	2015-03-05 03:45:01.000000000 +0800
+++ slime-2.13/slime.el	2015-04-12 14:30:27.901769989 +0800
@@ -138,16 +138,7 @@
     "Return the datestring of the latest entry in the ChangeLog file.
 Return nil if the ChangeLog file cannot be found."
     (interactive "p")
-    (let ((changelog (slime--changelog-file-name))
-          (date nil))
-      (when (file-exists-p changelog)
-        (with-temp-buffer
-          (insert-file-contents-literally changelog nil 0 100)
-          (goto-char (point-min))
-          (setq date (symbol-name (read (current-buffer))))))
-      (when interactivep
-        (message "Slime ChangeLog dates %s." date))
-      date)))
+    "@SLIME-CHANGELOG-DATE@"))
 
 (defvar slime-protocol-version nil)
 (setq slime-protocol-version
