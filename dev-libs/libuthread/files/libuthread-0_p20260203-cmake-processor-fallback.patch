--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -31,6 +31,10 @@
   COMMAND_ERROR_IS_FATAL ANY
 )
 string(REGEX MATCH "^[^-]+" NVUT_TARGET "${NVUT_TARGET}")
+string(STRIP "${NVUT_TARGET}" NVUT_TARGET)
+if("${NVUT_TARGET}" STREQUAL "")
+  set(NVUT_TARGET "${CMAKE_SYSTEM_PROCESSOR}")
+endif()
 message(STATUS "NVUT_TARGET = ${NVUT_TARGET}")

 option(NVUT_BUILD_TESTS "Build tests" ON)
