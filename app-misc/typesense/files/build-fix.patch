diff -Nur a/cmake/For.cmake b/cmake/For.cmake
--- a/cmake/For.cmake	2025-06-29 19:49:53.000000000 -0700
+++ b/cmake/For.cmake	2025-10-06 01:16:12.992571584 -0700
@@ -4,19 +4,19 @@
 set(FOR_NAME libfor-${FOR_VERSION})
 set(FOR_TAR_PATH ${DEP_ROOT_DIR}/${FOR_NAME}.tar.gz)
 
-if(NOT EXISTS ${FOR_TAR_PATH})
-    message(STATUS "Downloading libfor...")
-    file(DOWNLOAD https://github.com/cruppstahl/libfor/archive/${FOR_VERSION}.tar.gz ${FOR_TAR_PATH})
-endif()
+#if(NOT EXISTS ${FOR_TAR_PATH})
+#    message(STATUS "Downloading libfor...")
+#    file(DOWNLOAD https://github.com/cruppstahl/libfor/archive/${FOR_VERSION}.tar.gz ${FOR_TAR_PATH})
+#endif()
+#
+#if(NOT EXISTS ${DEP_ROOT_DIR}/${FOR_NAME})
+#    message(STATUS "Extracting libfor...")
+#    execute_process(COMMAND ${CMAKE_COMMAND} -E tar xzf ${FOR_TAR_PATH} WORKING_DIRECTORY ${DEP_ROOT_DIR})
+#endif()
 
-if(NOT EXISTS ${DEP_ROOT_DIR}/${FOR_NAME})
-    message(STATUS "Extracting libfor...")
-    execute_process(COMMAND ${CMAKE_COMMAND} -E tar xzf ${FOR_TAR_PATH} WORKING_DIRECTORY ${DEP_ROOT_DIR})
-endif()
-
-if(NOT EXISTS ${DEP_ROOT_DIR}/${FOR_NAME}/libfor.a AND BUILD_DEPS STREQUAL "yes")
+if(NOT EXISTS $ENV{FOR_DIR}/libfor.a AND BUILD_DEPS STREQUAL "yes")
     message("Building libfor locally...")
-    execute_process(COMMAND make WORKING_DIRECTORY ${DEP_ROOT_DIR}/${FOR_NAME}/
+    execute_process(COMMAND make WORKING_DIRECTORY $ENV{FOR_DIR}/
                     RESULT_VARIABLE FOR_BUILD)
     if(NOT FOR_BUILD EQUAL 0)
         message(FATAL_ERROR "${FOR_NAME} build failed!")
diff -Nur a/cmake/H2O.cmake b/cmake/H2O.cmake
--- a/cmake/H2O.cmake	2025-06-29 19:49:53.000000000 -0700
+++ b/cmake/H2O.cmake	2025-10-06 01:26:12.325970969 -0700
@@ -4,48 +4,49 @@
 set(H2O_NAME h2o-${H2O_VERSION})
 set(H2O_TAR_PATH ${DEP_ROOT_DIR}/${H2O_NAME}.tar.gz)
 
-if(NOT EXISTS ${H2O_TAR_PATH})
-    message(STATUS "Downloading https://github.com/h2o/h2o/archive/${H2O_VERSION}.tar.gz")
-    file(DOWNLOAD https://github.com/h2o/h2o/archive/${H2O_VERSION}.tar.gz ${H2O_TAR_PATH})
-endif()
-
-if(NOT EXISTS ${DEP_ROOT_DIR}/${H2O_NAME})
-    message(STATUS "Extracting ${H2O_NAME}...")
-    execute_process(COMMAND ${CMAKE_COMMAND} -E tar xzf ${H2O_TAR_PATH} WORKING_DIRECTORY ${DEP_ROOT_DIR}/)
-endif()
-
-if(NOT EXISTS ${DEP_ROOT_DIR}/${H2O_NAME}/build/libh2o-evloop.a)
-    message("Configuring ${H2O_NAME}...")
-    file(MAKE_DIRECTORY ${DEP_ROOT_DIR}/${H2O_NAME}/build)
+#if(NOT EXISTS ${H2O_TAR_PATH})
+#    message(STATUS "Downloading https://github.com/h2o/h2o/archive/${H2O_VERSION}.tar.gz")
+#    file(DOWNLOAD https://github.com/h2o/h2o/archive/${H2O_VERSION}.tar.gz ${H2O_TAR_PATH})
+#endif()
+#
+#if(NOT EXISTS ${DEP_ROOT_DIR}/${H2O_NAME})
+#    message(STATUS "Extracting ${H2O_NAME}...")
+#    execute_process(COMMAND ${CMAKE_COMMAND} -E tar xzf ${H2O_TAR_PATH} WORKING_DIRECTORY ${DEP_ROOT_DIR}/)
+#endif()
+
+if(NOT EXISTS $ENV{H2O_DIR}/build/libh2o-evloop.a)
+    message("Configuring $ENV{H2O_DIR}...")
+    file(MAKE_DIRECTORY $ENV{H2O_DIR}/build)
 
     execute_process(COMMAND ${CMAKE_COMMAND} -E copy_directory deps/picotls/include include/h2o
-                    WORKING_DIRECTORY ${DEP_ROOT_DIR}/${H2O_NAME})
+	WORKING_DIRECTORY $ENV{H2O_DIR})
 
     execute_process(COMMAND ${CMAKE_COMMAND} -E copy_directory deps/quicly/include include/h2o
-            WORKING_DIRECTORY ${DEP_ROOT_DIR}/${H2O_NAME})
+	WORKING_DIRECTORY $ENV{H2O_DIR})
 
     execute_process(COMMAND ${CMAKE_COMMAND}
+            -DCMAKE_POLICY_VERSION_MINIMUM=3.5
             -DCMAKE_FIND_LIBRARY_SUFFIXES=.a
             -DCMAKE_CXX_FLAGS="-DNDEBUG"
             -DCMAKE_C_FLAGS="-DNDEBUG"
             -DWITH_MRUBY=off
-            "-H${DEP_ROOT_DIR}/${H2O_NAME}"
-            "-B${DEP_ROOT_DIR}/${H2O_NAME}/build"
+	    "-H$ENV{H2O_DIR}"
+	    "-B$ENV{H2O_DIR}/build"
             RESULT_VARIABLE
             H2O_CONFIGURE)
     if(NOT H2O_CONFIGURE EQUAL 0)
-        message(FATAL_ERROR "${H2O_NAME} configure failed!")
+	message(FATAL_ERROR "$ENV{H2O_DIR} configure failed!")
     endif()
 
     if(BUILD_DEPS STREQUAL "yes")
-        message("Building ${H2O_NAME} locally...")
+	message("Building $ENV{H2O_DIR} locally...")
         execute_process(COMMAND ${CMAKE_COMMAND} --build
-                "${DEP_ROOT_DIR}/${H2O_NAME}/build"
+	    "$ENV{H2O_DIR}/build"
                 --target libh2o-evloop
                 RESULT_VARIABLE
                 H2O_BUILD)
         if(NOT H2O_BUILD EQUAL 0)
-            message(FATAL_ERROR "${H2O_NAME} build failed!")
+	    message(FATAL_ERROR "$ENV{H2O_DIR} build failed!")
         endif()
     endif()
 endif()
