--- a/cmake/ApacheArrow.cmake
+++ b/cmake/ApacheArrow.cmake
@@ -1,3 +1,8 @@
+# Path to a pre-fetched Arrow source tree (set via -DARROW_SOURCE_DIR=...)
+if(NOT DEFINED ARROW_SOURCE_DIR OR ARROW_SOURCE_DIR STREQUAL "")
+	message(FATAL_ERROR "ARROW_SOURCE_DIR must be set to the pre-fetched Arrow source directory")
+endif()
+
 add_library(apache_arrow_static INTERFACE)
 add_library(arrow_static STATIC IMPORTED)
 add_library(parquet_static STATIC IMPORTED)
@@ -18,22 +23,17 @@ endif()
 include(ExternalProject)
 ExternalProject_Add(
     apache-arrow-cpp
     PREFIX ${PREFIX}
-    GIT_REPOSITORY https://github.com/apache/arrow.git
-    GIT_TAG b7d2f7ffca66c868bd2fce5b3749c6caa002a7f0
-    GIT_SHALLOW ON
-    GIT_PROGRESS ON
-    GIT_SUBMODULES ""
+    SOURCE_DIR "${ARROW_SOURCE_DIR}"
     SOURCE_SUBDIR "cpp"
     BUILD_IN_SOURCE ON
     INSTALL_DIR ${PREFIX}
-    CONFIGURE_COMMAND bash -x -c "\
-    ( cd thirdparty && [[ -f export.sh ]] || ./download_dependencies.sh | tee export.sh ) && \
-    source thirdparty/export.sh && cmake -S . -B . \
-        -DCMAKE_BUILD_TYPE=Release \
-        -DARROW_USE_CCACHE=OFF \
-        -DARROW_USE_SCCACHE=OFF \
-        -DARROW_DEPENDENCY_SOURCE=BUNDLED \
-        -DARROW_BUILD_STATIC=ON \
-        -DARROW_JEMALLOC=ON \
-        -DARROW_SIMD_LEVEL=DEFAULT \
-        -DARROW_BUILD_EXAMPLES=OFF \
-        -DARROW_PARQUET=ON -DARROW_CSV=ON \
-        -DARROW_WITH_ZSTD=ON -DARROW_WITH_LZ4=ON -DARROW_WITH_ZLIB=ON"
-    BUILD_COMMAND bash -x -c "source thirdparty/export.sh && cmake --build . ${ARROW_BUILD_PARALLEL}"
+    CONFIGURE_COMMAND cmake -S . -B .
+        -DCMAKE_BUILD_TYPE=Release
+        -DARROW_USE_CCACHE=OFF
+        -DARROW_USE_SCCACHE=OFF
+        -DARROW_DEPENDENCY_SOURCE=SYSTEM
+        -DARROW_BUILD_STATIC=ON
+        -DARROW_JEMALLOC=OFF
+        -DARROW_SIMD_LEVEL=DEFAULT
+        -DARROW_BUILD_EXAMPLES=OFF
+        -DARROW_PARQUET=ON
+        -DARROW_CSV=ON
+        -DARROW_WITH_ZSTD=ON
+        -DARROW_WITH_LZ4=ON
+        -DARROW_WITH_ZLIB=ON
+    BUILD_COMMAND cmake --build . ${ARROW_BUILD_PARALLEL}
     BUILD_JOB_SERVER_AWARE 1
